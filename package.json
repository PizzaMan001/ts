{
  "name": "hls",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy index.js --name hls --compatibility-date=2026-03-28",
    "dev": "wrangler dev index.js"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
